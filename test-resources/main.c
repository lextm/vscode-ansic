#include <stdio.h>

int main() {
    int result, code = 1;
    printf("Hello, World!\n");
    result = -1 + code;
    int nextTest = -2;
    break1();

    return test(result);
}

int test(short error) {
    return error;
}
